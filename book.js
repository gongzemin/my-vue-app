class Library {
  constructor() {
    // Initialize library with an empty array to store books
    this.books = []
  }

  // Method to add a book to the library
  addBook(book) {
    this.books.push(book)
  }

  // Method to borrow a book
  borrowBook(bookTitle) {}
}
